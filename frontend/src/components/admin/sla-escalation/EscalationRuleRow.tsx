import { ReactNode } from 'react';
import { EscalationRule } from '../../../services/serviceDesk.service';
import { roleDisplayName } from './RoleChipPicker';
export function EscalationRuleRow({ rule, onEdit, onToggleActive, onDelete, isEditing, editForm }: { rule: EscalationRule; onEdit: (rule: EscalationRule) => void; onToggleActive: (rule: EscalationRule) => void; onDelete: (rule: EscalationRule) => void; isEditing: boolean; editForm?: ReactNode }) {
    return <div className={`border-l-4 bg-white ${rule.isActive ? 'border-emerald-400' : 'border-gray-300'} ${isEditing ? 'rounded-xl shadow-sm' : ''}`}><div className="flex flex-wrap items-center justify-between gap-3 px-4 py-4"><div><div className="flex items-center gap-2"><span className="rounded-full bg-blue-100 px-2.5 py-0.5 text-sm font-black text-blue-800">+{rule.triggerHoursAfterBreach}h</span><span className="text-sm font-bold text-[#101418]">after breach</span>{rule.label && <span className="text-sm text-[#8993a4]">— {rule.label}</span>}</div><div className="mt-2 flex flex-wrap gap-1.5">{rule.notifyRoles.map(role => <span key={role} className="rounded-full bg-gray-100 px-2 py-0.5 text-[11px] font-bold text-[#44546f]">{roleDisplayName(role)}</span>)}</div></div><div className="flex items-center gap-2"><button type="button" onClick={() => onToggleActive(rule)} className={`rounded-lg px-3 py-1.5 text-xs font-bold ${rule.isActive ? 'bg-emerald-50 text-emerald-700' : 'bg-gray-100 text-gray-500'}`}>{rule.isActive ? 'Active' : 'Disabled'}</button><button type="button" onClick={() => onEdit(rule)} className="rounded-lg p-2 text-[#0052cc] hover:bg-blue-50" aria-label={`Edit rule +${rule.triggerHoursAfterBreach} hours`}><span className="material-symbols-outlined text-lg">edit</span></button><button type="button" onClick={() => onDelete(rule)} className="rounded-lg p-2 text-red-500 hover:bg-red-50" aria-label={`Delete rule +${rule.triggerHoursAfterBreach} hours`}><span className="material-symbols-outlined text-lg">delete</span></button></div></div>{isEditing && editForm && <div className="border-t border-gray-100 bg-gray-50/60 p-4">{editForm}</div>}</div>;
}
