import React from 'react';

/**
 * Re-export for backward compatibility.
 * RiskBadge has been moved to components/ui/RiskBadge.tsx
 * with universal risk rating color mappings.
 */
export { default, default as RiskBadge } from '../ui/RiskBadge';