import { useState } from 'react';
import type { WorkflowSummary, WorkflowVersionSummary, ValidationFinding } from '../../services/workflow-version.service';

interface PublishDialogProps { workflow: WorkflowSummary; version: WorkflowVersionSummary; blocking: ValidationFinding[]; warnings: ValidationFinding[]; busy: boolean; onConfirm: () => void; onClose: () => void; }
export default function PublishDialog({ workflow, version, blocking, warnings, busy, onConfirm, onClose }: PublishDialogProps) {
  const [accepted, setAccepted] = useState(warnings.length === 0);
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#101418]/40 p-4" role="presentation" onMouseDown={onClose}><div className="w-full max-w-xl rounded-2xl bg-white p-6 shadow-2xl" role="dialog" aria-modal="true" aria-labelledby="publish-title" onMouseDown={(event) => event.stopPropagation()}><h2 id="publish-title" className="text-xl font-black text-[#101418]">Publish workflow v{version.version}</h2><p className="mt-2 text-sm text-[#44546f]">This will activate the new version of <strong>{workflow.name}</strong> for {workflow.requestTypes.length} request type{workflow.requestTypes.length === 1 ? '' : 's'}.</p>{blocking.length > 0 && <div className="mt-4 rounded-lg bg-[#fff0f0] p-3 text-sm text-[#b42318]"><strong>Publishing is blocked.</strong> Resolve {blocking.length} blocking finding{blocking.length === 1 ? '' : 's'} first.</div>}{warnings.length > 0 && <label className="mt-4 flex gap-2 rounded-lg bg-[#fff4d6] p-3 text-sm text-[#8a5a00]"><input type="checkbox" checked={accepted} onChange={(event) => setAccepted(event.target.checked)} /> I accept the {warnings.length} validation warning{warnings.length === 1 ? '' : 's'}.</label>}<div className="mt-6 flex justify-end gap-3"><button className="rounded-lg border border-[#b9c8de] px-4 py-2 text-sm font-semibold text-[#334a70]" onClick={onClose}>Cancel</button><button className="rounded-lg bg-[#0052cc] px-4 py-2 text-sm font-semibold text-white disabled:opacity-50" disabled={busy || blocking.length > 0 || !accepted} onClick={onConfirm}>{busy ? 'Publishing…' : 'Publish version'}</button></div></div></div>;
}
